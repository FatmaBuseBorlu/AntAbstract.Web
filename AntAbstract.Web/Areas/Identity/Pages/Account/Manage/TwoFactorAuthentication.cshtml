@page
@using System
@using Microsoft.AspNetCore.Http.Features
@model TwoFactorAuthenticationModel
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

@{
    Func<string, string, string> L = (key, fallback) =>
    {
        var text = Localizer[key].Value;
        return string.Equals(text, key, StringComparison.OrdinalIgnoreCase) ? fallback : text;
    };

    ViewData["Title"] = L("PageTitle", "İki Faktörlü Doğrulama (2FA)");
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="container" style="max-width:720px">
    <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body p-4">
            <div class="d-flex align-items-center gap-3 mb-4">
                <div style="width:48px;height:48px;border-radius:14px;background:linear-gradient(135deg,#7c3aed,#a78bfa);display:flex;align-items:center;justify-content:center;">
                    <i class="fas fa-shield-halved text-white fs-5"></i>
                </div>
                <div>
                    <h4 class="fw-bold mb-0">@ViewData["Title"]</h4>
                    <p class="text-muted mb-0 small">@L("Subtitle", "Hesabınızın güvenliğini artırın")</p>
                </div>
            </div>

@{
    var consentFeature = HttpContext.Features.Get<ITrackingConsentFeature>();

    if (consentFeature?.CanTrack ?? true)
    {
        if (Model.Is2faEnabled)
        {
            <div class="alert alert-success border-0 rounded-3 d-flex align-items-center gap-2 mb-4">
                <i class="fas fa-check-circle fs-5"></i>
                <div>
                    <strong>@L("TwoFAActive", "2FA Aktif")</strong>
                    <span class="text-muted ms-1">— @L("TwoFAActiveDescription", "Hesabınız iki faktörlü doğrulama ile korunuyor.")</span>
                </div>
            </div>

            if (Model.RecoveryCodesLeft == 0)
            {
                <div class="alert alert-danger border-0 rounded-3 mb-3">
                    <i class="fas fa-exclamation-triangle me-1"></i>
                    <strong></strong>
                    <a asp-page="./GenerateRecoveryCodes" class="alert-link">@L("GenerateNewCodes", "Yeni kodlar oluşturun")</a>.
                </div>
            }
            else if (Model.RecoveryCodesLeft <= 3)
            {
                <div class="alert alert-warning border-0 rounded-3 mb-3">
                    <i class="fas fa-exclamation-circle me-1"></i>
                    <strong>@Model.RecoveryCodesLeft </strong>
                    <a asp-page="./GenerateRecoveryCodes" class="alert-link">@L("GenerateNewCodes", "Yeni kodlar oluşturun")</a>.
                </div>
            }

            <div class="d-flex gap-2 flex-wrap mb-4">
                @if (Model.IsMachineRemembered)
                {
                    <form method="post" class="d-inline">
                        <button type="submit" class="btn btn-outline-secondary btn-sm">
                            <i class="fas fa-desktop me-1"></i> @L("ForgetThisDevice", "Bu Cihazı Unut")
                        </button>
                    </form>
                }
                <a asp-page="./Disable2fa" class="btn btn-outline-danger btn-sm">
                    <i class="fas fa-power-off me-1"></i> @L("Disable2FA", "2FA Devre Dışı Bırak")
                </a>
                <a asp-page="./GenerateRecoveryCodes" class="btn btn-outline-warning btn-sm">
                    <i class="fas fa-key me-1"></i> @L("ResetRecoveryCodes", "Kurtarma Kodlarını Yenile")
                </a>
                <a asp-page="./ResetAuthenticator" class="btn btn-outline-secondary btn-sm">
                    <i class="fas fa-sync me-1"></i> @L("ResetAuthenticator", "Authenticator Sıfırla")
                </a>
            </div>
        }
        else
        {
            <div class="alert alert-warning border-0 rounded-3 d-flex align-items-center gap-2 mb-4">
                <i class="fas fa-shield-halved fs-5"></i>
                <div>
                    <strong></strong>
                    <span class="text-muted ms-1">— @L("PasswordOnlyDescription", "Hesabınız sadece şifre ile korunuyor.")</span>
                </div>
            </div>
        }

        <div class="border rounded-3 p-4 text-center" style="background:#f8fafc;">
            @if (!Model.HasAuthenticator)
            {
                <i class="fas fa-mobile-alt fa-3x text-primary mb-3 d-block"></i>
                <h6 class="fw-bold">@L("AddAuthenticatorTitle", "Authenticator Uygulaması Ekleyin")</h6>
                <p class="text-muted small mb-3">
                    
                </p>
                <a asp-page="./EnableAuthenticator" class="btn btn-primary px-4">
                    <i class="fas fa-plus me-1"></i> @L("AddAuthenticatorButton", "Authenticator Ekle")
                </a>
            }
            else
            {
                <i class="fas fa-check-circle fa-3x text-success mb-3 d-block"></i>
                <h6 class="fw-bold">@L("AuthenticatorConnected", "Authenticator Bağlı")</h6>
                <p class="text-muted small mb-3">
                    
                </p>
                <a asp-page="./EnableAuthenticator" class="btn btn-outline-primary px-4">
                    <i class="fas fa-cog me-1"></i> @L("ReconfigureButton", "Yeniden Yapılandır")
                </a>
            }
        </div>
    }
    else
    {
        <div class="alert alert-danger border-0 rounded-3">
            <i class="fas fa-cookie-bite me-1"></i>
            <strong></strong>
            
        </div>
    }
}
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
