@using AntAbstract.Domain.Entities
@model IEnumerable<Message>

@{
    ViewData["Title"] = "Gelen Kutusu";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="display-5">@ViewData["Title"]</h1>
    <div>
        <a asp-action="Create" class="btn btn-primary">Yeni Mesaj Oluştur</a>
        <a asp-action="Outbox" class="btn btn-secondary">Giden Kutusuna Git</a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        Gelen kutunuzda hiç mesaj bulunmamaktadır.
    </div>
}
else
{
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Gönderen</th>
                <th>Konu</th>
                <th>Tarih</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="@(item.IsRead ? "" : "fw-bold")">
                    <td>@item.Sender.Email</td>
                    <td>@item.Subject</td>
                    <td>@item.SentDate.ToLocalTime().ToString("dd MMM yyyy, HH:mm")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Oku</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}